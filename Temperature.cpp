#include <iostream>

 using namespace std; 
 
int main () {
	  int temperature; 
	    cout << "Enter the temperature: ";
	    cin  >> temperature;
	  if (temperature > 32); { 
	        cout << "Bring heavy jacket." << endl; 
    } if else (temperature  >= 32 && temperature <= 50) {
	        cout << "Bring a light jakcet." << endl;
    } else  {  (temperature <= 50);
	        cout << "Do not bring any jacket." << endl; 
    } 
	 return 0;
}	 
